apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-app-db-kafka.fullname" . }}-test-connection"
  labels:
    {{- include "demo-app-db-kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-app-db-kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
